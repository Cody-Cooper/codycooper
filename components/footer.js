const Footer = (props) => (
  <footer
    sx={{
      display:
        /* "flex", */ "none" /*hiding footer until i have good enough copy/links to want to put it on*/,
      alignItems: "center",
      justifyContent: "center",
      variant: "styles.header",
    }}
  >
    Footer
  </footer>
);

export default Footer;
